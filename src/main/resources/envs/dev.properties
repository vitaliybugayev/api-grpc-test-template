# Development environment configuration
api.host=https://api-dev.example.com
api.version=v1

# gRPC services configuration
user.service.host=localhost
user.service.port=9001
grpc.deadline.seconds=5

order.service.host=localhost
order.service.port=9002

# Other service endpoints
external.service.url=https://external-dev.example.com
